import { motion } from 'framer-motion';
import classes from './CallMeButton.module.css'
function CallMeButton() {

    return (
        <button
            className={classes.button}>Напиши мне</button>);
}

export default CallMeButton;