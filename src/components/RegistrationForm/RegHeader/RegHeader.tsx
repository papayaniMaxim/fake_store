import classes from "./RegHeader.module.css";

function RegHeader() {
  return (
    <header className={classes.headerContainer}>
      <h1 className={classes.title}>REGISTRATION FORM</h1>
    </header>
  );
}

export default RegHeader;
